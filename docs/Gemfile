source "https://rubygems.org"

# Use github-pages gem to match GitHub Pages environment exactly
gem "github-pages", "~> 232", group: :jekyll_plugins

# Additional plugins (github-pages already includes jekyll-seo-tag)
group :jekyll_plugins do
  gem "jekyll-github-metadata", "~> 2.16"
end
